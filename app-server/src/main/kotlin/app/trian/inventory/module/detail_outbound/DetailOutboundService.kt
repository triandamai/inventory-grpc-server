package app.trian.inventory.module.detail_outbound

import org.springframework.stereotype.Service

@Service
class DetailOutboundService(
    private val detailOutboundRepository: DetailOutboundRepository
) {
}